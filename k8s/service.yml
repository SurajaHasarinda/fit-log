apiVersion: v1
kind: Service
metadata:
  name: fit-log-service
  namespace: fit-log
  labels:
    app: fit-log
spec:
  type: LoadBalancer
  selector:
    app: fit-log
  ports:
    - port: 80
      targetPort: 9281
